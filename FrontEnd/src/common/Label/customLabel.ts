import styled from "styled-components";

export const StyledLabel = styled.label`
  font-family: 'Motiva Sans Light', sans-serif; // This matches your body font
  color: #000000; // Adjust the color as needed
  font-size: 21px; // This matches your global p style
  line-height: 1.41; // This matches your global p style
  margin-bottom: 0.5rem; // Add some space below the label
  display: block; // Ensures it takes the full width
`;
