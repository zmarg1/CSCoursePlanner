// config.js
const Config = {
    backendURL: 'https://zmarg1.pythonanywhere.com', // Change to this when ready to host
    //backendURL: `http://127.0.0.1:5000` // Hosted locally
};

export default Config;